---
description: Enforce proper ArrayList syntax
alwaysApply: true
---
# Overview

- Use `std.ArrayList(T)` and initialize with `std.ArrayList(T).empty`
- Do not use deprecated `init()` methods from older Zig versions
